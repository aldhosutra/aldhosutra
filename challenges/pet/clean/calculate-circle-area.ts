// REASON: This code is clean because it has a descriptive name, clear variable names, and performs a single, obvious task: calculating the area of a circle.
export function calculateCircleArea(radius: number): number {
	const PI = 3.14159;
	return PI * radius * radius;
}
