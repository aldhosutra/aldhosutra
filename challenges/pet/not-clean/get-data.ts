// REASON: This code is not-clean. It contains a large, commented-out block of old code, which creates clutter and confusion about what is currently in use.
export function getData(id: number) {
	// const url = '...';
	// fetch(url).then(...);
	// let old_logic = 1;
	// if (id > 0) return old_logic;
	return `item-${id}`;
}
