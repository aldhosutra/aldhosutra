// REASON: This code is warm because it immediately returns a static list of items. The data is ready and does not need to be loaded from anywhere.
export function getTreatOptions() {
	return ['tuna', 'chicken', 'salmon'];
}
